<!-- ! This Keyword -->
In JavaScript, the this keyword refers to an object, but which object it refers to depends on how the function is called, not where it is defined (except for arrow functions).
