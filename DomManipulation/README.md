
DOM in JavaScript

DOM (Document Object Model) is a programming interface that allows JavaScript to access, modify, add, and delete HTML elements and their content.

In simple words:

HTML creates the webpage → DOM represents the webpage as a tree → JavaScript manipulates that tree.