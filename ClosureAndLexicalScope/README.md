
<!-- ! Lexical scope -->
Lexical scope (also called static scope) in JavaScript means that a variable's accessibility is determined by where it is declared in the source code, not by where a function is called.

<!-- ! Closure in JavaScript -->
A closure is created when a function remembers and can access variables from its outer (lexical) scope even after the outer function has finished executing.

